<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/global.css">
        <link rel="stylesheet" href="/css/useredits.css">
        <title>Editar Usuario</title>
    </head>
    <body>
        <h1>Datos personales</h1>
        <form action="/update_user" method="POST">
            <label for="name">Nombre:</label>
            <input type="text" id="name" name="name" value="{{user.name}}" autocomplete="off" required>

            <label for="email">Correo:</label>
            <input type="email" id="email" name="email" value="{{user.email}}" autocomplete="off" required>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Dejar en blanco para no cambiar" autocomplete="off">

            <button type="submit">Guardar cambios</button>
        </form>

        <a href="/">Cancelar</a>
    </body>
</html>
